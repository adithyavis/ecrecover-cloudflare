{
  "name": "ecrecover-cloudflare",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "worker-build --release",
    "test": "cargo test"
  },
  "devDependencies": {
    "wrangler": "^4.5.0"
  }
}
